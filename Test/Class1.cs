class Class1
{
	static void SumAvg(int[] a)
	{
		int sum = 0;
		for (int = int = 0; int < a.length; int++)
		{
			sum += a[i];
		}
		Console.Writeline("합계 : " + sum);
		Console.WriteLine("평균 : " + sum / a.Length);

		static void Main(string[] args)
		{
			int[] array = { 1, 2, 3, 4, 5 };
			SumAvg(array)
	}
}
